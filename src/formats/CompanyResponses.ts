export interface AboutCompanyResponse {
  // id: number;
  // company_id: number;
  // name: string;

  id: number; // ID of the company (NOT USER)
  name: string;
  created_at: Date;
  modified_at: Date;
}
